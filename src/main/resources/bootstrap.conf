consul {
    client {
        defaultZone="${CONSUL_HOST:localhost}:${CONSUL_PORT:8500}"
    }
}
micronaut {
    application {
        name=micronautDemo
    }
    config-client {
        enabled=true
    }
}
